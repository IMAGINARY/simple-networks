.helper
  div(style="flex:1;") Show gradient of loss function
    label.switch
      input(type="checkbox")#showgradient
      span.slider.round
  .h-seperator
  div(style="flex:1;") Train with gradient descent
    .controls
      div(title="reset training").reset
      div(title="pause/resume training (continious gradient descent steps)").pause-resume.pause
      div(title="train a single step (Adjust parameters with gradient descent)").single-step
.box
  h3 Training Data
  table#trainingtable
  .v-seperator
  div
    h3 Total error on training data
    p Value of the loss function (to be minimized):
    .center-formula
      p#totalerror
      p.formula
        span  = 
        span#totalerrorterm
