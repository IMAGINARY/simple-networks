nodes:
  - 'in'
  - 'out'

edges:
  - 'in -> out'

properties:
  'in':
    input:
      value: 1
      range:
        min: -1
        max: 1
  'out':
    bias:
      train: false

training:
  outputs:
    out: '2*in'
