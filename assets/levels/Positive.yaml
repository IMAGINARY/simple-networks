edges:
  - 'in -> h'
  - 'h -> out'

properties:
  'in':
    input:
      value: 1
      range:
        min: -1
        max: 1
  'h':
    bias:
      train: false
  'out':
    bias:
      train: false

training:
  outputs:
    out: 'in > 0 ? in : 0'
